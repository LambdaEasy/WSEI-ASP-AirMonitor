using AirMonitor.Core.Installation;

namespace AirMonitor.Core
{
    // for now it is only wrapper on IInstallationFacade, it might change through
    public interface IIntegrationFacade : IInstallationFacade { }
}
